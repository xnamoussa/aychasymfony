{% extends 'admin/base_admin.html.twig' %}

{% block title %}Modifier événement{% endblock %}

{% block body %}
    <h1 class="mt-4">Modifier : {{ evenement.titre }}</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="{{ path('app_evenement_index') }}">Événements</a></li>
        <li class="breadcrumb-item active">Mettre Ã  jour</li>
    </ol>
    
    <div class="row">
        <div class="col-lg-8">
            <div class="card mb-4 bg-dark text-white shadow-sm border-0">
                <div class="card-header border-secondary border-bottom">
                    <i class="fas fa-edit me-1"></i> Mise Ã  jour des informations
                </div>
                <div class="card-body">
                    {{ include('evenement/_form.html.twig', {'button_label': 'METTRE Ã€ JOUR'}) }}
                </div>
            </div>
            
            <div class="mt-4 text-end">
                {{ include('evenement/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
