{% extends 'base.html.twig' %}

{% block title %}Modifier Ticket - LAMMA{% endblock %}
{% block page_title %}Mise à jour Ticket{% endblock %}

{% block body %}
<section class="crud-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card card-detail">
                    <div class="card-header">Détails de modification</div>
                    <div class="card-body form-section">
                        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                        <div class="row">
                            <div class="col-12">
                                {{ form_widget(form) }}
                            </div>
                        </div>
                        <div class="mt-4 text-center">
                            <button class="btn btn-add">Mettre à jour</button>
                            <a href="{{ path('app_ticket_index') }}" class="btn btn-secondary" style="border-radius:25px;padding:10px 30px;">Annuler</a>
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
